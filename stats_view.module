<?php
// $Id$

/**
 * @file
 * Display usage statistics in various places
 * @author Bèr Kessels <ber@webschuur.com>
 * @link http://wizzlern.nl
 * @package stats_view
 */

/* -----------------------------------------------------------------
  Hook Implementations
------------------------------------------------------------------ */
/**
 * Implementation of hook_menu().
 */
function stats_view_menu() {
  $items = array();

  $items['admin/settings/stats_view'] = array(
    'title' => 'Stats view configuration',
    'page callback' => 'stats_view_settings',
    'access arguments' => array('access administration pages'), //Komen we later op terug.
  );

  return $items;
}

/** 
 * Builds the HTML of a settings-form.
 * Callback for hook_menu().
 *
 * @return
 *   string HTML for settings form
 */
function stats_view_settings() {
  return drupal_get_form('stats_view_settings_form');
}

/**
 * Callback for drupal_get_form in stats_view_settings.
 */
function stats_view_settings_form() {

  // @TODO form item with:
  //        type: textfield,
  //        title 'Amount of entries to show'
  //        description: 'Provide the amount of statistics-entries that you want to show in the block with the articles.'
  //      ' default_value: 5
  //        weight: -100

  // @TODO form item with:
  //        type: submit
  //        text on the button: 'Save'

  return $form;
}

/**
 * @TODO: introduce the validate function for stats_view_settings_form
 * Validation callback for stats_view_settings form.
 */
// @TODO the function should check if the value a user provide is
//       a number, if not, set an error explaining the user he/she needs to enter a round number.
//       positive, if not, set an error explaining the user he/she needs to enter a positive number.

/**
 * @TODO: introduce the submit handler function for stats_view_settings_form
 * Submit callback for stats_view_settings form.
 */
// @TODO, in that function, use variable_set() to store the amount of entries to be shown.

