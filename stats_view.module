<?php
// $Id$

/**
 * @file
 * Display usage statistics in various places
 * @author Bèr Kessel <ber@webschuur.com>
 * @link http://wizzlern.nl
 * @package stats_view
 */

/* -----------------------------------------------------------------
  Hook Implementations
------------------------------------------------------------------ */
/**
 * Implementation of hook_menu().
 */
function stats_view_menu() {
  $items = array();

  $items['piraten'] = array(
    'title' => 'Piraat zegt',
    'page callback' => 'stats_view_hello',
    'access arguments' => array('access administration pages'), //Komen we later op terug.
  );

  return $items;
}

/**
 * Callback for hook_menu().
 * 'page callback' => 'stats_view_settings' causes this function to be ran
 * for output on the page admin/settings/stats_view.
 */
function stats_view_hello() {
  return "Arrr, wecome on board, Ye Drupaleers. Let us get some modules de'eloped else ye'll ne'er get me buried booty!";
}
